# Results

This directory contains final result artifacts used in the thesis:

- `tables/`: summary CSV files with corpus-level metrics,
- `figures/`: final PNG plots,
- `zero_shot/`: selected zero-shot outputs,
- `fine_tuning/`: selected fine-tuning outputs.

One-off qualitative probe scripts and temporary analysis files are not included.
